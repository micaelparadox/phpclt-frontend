import React from 'react';

const CommandList = () => (
  <div className="p-4">
    <h2 className="text-2xl font-bold">Command list</h2>
    {/* Add your command list content here, similar to the HTML table structure */}
  </div>
);

export default CommandList;
